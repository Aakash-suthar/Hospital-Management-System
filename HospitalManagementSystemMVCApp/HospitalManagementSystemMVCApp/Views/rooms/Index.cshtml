@model IEnumerable<HospitalManagementSystemMVCApp.Models.room>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.room_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.room_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.patient_id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.room_charge)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.room_id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.room_name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.patient_id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.room_charge)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.room_id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.room_id })
    </td>
</tr>
}

</table>
